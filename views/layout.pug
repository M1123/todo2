extends ../node_modules/jade-bootstrap/_bootstrap
block  body
    br
    nav
      +navbar("ToDo","dropdown_menu")
        +nav_item("/","active") Главная
        +nav_item("/users") Users
        +nav_divider
        +nav_item("/logout") Выход
        
    block content